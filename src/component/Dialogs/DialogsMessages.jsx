import React from 'react';

const DialogsMessages = (props) => {
    debugger
    return (
        <div>
            аыф
            {props.displayName}
        </div>
    );
};

export default DialogsMessages;